# Portfolio Layouts

Astro layouts for the portfolio site. The base layout imports global styles, sets up `<head>` metadata, and provides the semantic HTML shell.

These layouts will be created during Phase 4 of the build.
