# Portfolio Components

Astro components for the portfolio site. Each section of the page has its own component with scoped SCSS styles.

These components will be created during Phases 4–6 of the build.
