// @ts-check
import { defineConfig } from 'astro/config'

// https://astro.build/config
export default defineConfig({
  site: 'https://your-username.github.io',
  // Uncomment if deploying to a subpath, e.g. https://user.github.io/portfolio
  // base: '/portfolio',
})
